# Complex Number Addition

This project adds two complex numbers using a typedef struct to represent them and a dedicated function to perform and display the addition.

## What I Learned
- How to represent complex numbers using a struct
- How to add complex numbers by adding real and imaginary parts separately
- How to use typedef struct for cleaner syntax
- How to pass structs to functions
